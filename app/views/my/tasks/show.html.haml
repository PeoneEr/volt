.row
  .col-xs-12
    %h1= %(№#{@task.id}, #{@task.name})
    %h2= I18n.l(@task.created_at, format: :long)
    %h2= simple_format(@task.description)
    - if current_user.admin?
      %h3= @task.user.email

    .row
      .col-xs-4
        = show_file(@task)
